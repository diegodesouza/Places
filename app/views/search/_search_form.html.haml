= form_tag listings_path, remote: true, method: :get, class:"search", id:"listing-search" do
  = text_field_tag :search, params[:search], :class => 'input'
  = submit_tag "Search", :name => nil, class: "search__button"
